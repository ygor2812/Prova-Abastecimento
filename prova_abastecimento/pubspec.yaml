name: prova_abastecimento
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1  # Versão mais recente; verifique pub.dev
firebase_auth: ^5.3.1
cloud_firestore: ^5.4.1
provider: ^6.1.2  # Para gerenciamento de estado (usaremos Provider para MVVM-like)
fl_chart: ^0.70.0  # Para gráficos (bônus)

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
